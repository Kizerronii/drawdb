{
  "name": "drawdb-studio-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Filesystem storage API dla drawdb studio (faza 1). REST + SSE nad ~/drawdb-projects/*.drawdb.json.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "chokidar": "^4.0.1",
    "fastify": "^5.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
